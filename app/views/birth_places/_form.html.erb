<%= form_for(@birth_place) do |f| %>
  <% if @birth_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@birth_place.errors.count, "error") %> prohibited this birth_place from being saved:</h2>

      <ul>
      <% @birth_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :birth %><br>
    <%= f.text_field :birth %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
